Create a new directory to record the changes and publish logs in `layer-esm`.
Write an article to introduce the new library for the dialog layer.
The key content is for the basic usage of `msg`, `confirm`, and `load`.
Supply the content to migrate from the legacy layer implementation to the new layer-esm.
